import { createTheme } from "@mui/material";


const theme = createTheme({
    palette: {
        orange: {
            primary: '#D87D4A',
            secondary: '#FBAF85'
        }
    },
    typography: {
        fontFamily: 'Manrope'
    }
});

export default theme;